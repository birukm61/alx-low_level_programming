#include <stdio.h>

/**
 * main - check the code for ALX School students.
 * reset_to_98 - resting to 98
 * Return: Always 0.
 *  *n - value to be reset
 */
void reset_to_98(int *n);
{
    int n;
    int n=&n;
    n = 402;
    printf("n=%d\n", n);
    *n = 98;
    printf("n=%d\n", n);
    return (0);
}
